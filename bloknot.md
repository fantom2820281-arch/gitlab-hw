### oсновные команды для работы с git 
git init
git status
git add имя_файла
git commit -m "some useful comment here"
git config --global

git diff
git diff --staged

Посмотрите изменения в файле README.md, выполнив команды git diff и git diff --staged.
Переведите файл в состояние staged или, как говорят, добавьте файл в коммит, командой git add README.md.

git add название файла

✅ Что делать: удалить файлы из истории Git (но оставить локально)
Шаг 1: Добавь файлы в .gitignore
Шаг 2: Удали файлы из индекса Git (но не с диска!)
git rm --cached key.json
git rm -r --cached terraform/  # если нужно целую папку
Шаг 3: Сделай новый коммит
git add .gitignore
git commit -m "Удалены секреты из репозитория, добавлены в .gitignore"
Шаг 4: Отправь изменения
git push origin main
⚠️ Важно!

    Этот способ удаляет файлы только из будущих коммитов, но они остаются в истории.
    Если файл содержит секреты (например, key.json) — его нужно полностью удалить из всей истории, иначе он останется доступен по старым коммитам.

🔒 Если файл — секретный (ключ, пароль и т.д.)
Тогда нужно переписать историю с помощью git filter-repo:
Установи git-filter-repo:
sudo apt install git-filter-repo -y

git filter-repo --path key.json --invert-paths

git push origin main --force

    ⚠️ Это изменит всю историю — делай так только в личном репозитории, где никто больше не работает.

📌 Итог